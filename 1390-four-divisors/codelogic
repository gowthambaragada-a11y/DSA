In these code :-
  for fast approach we can run inner loop for square root of i time
  but here is the problem is 
    we will run square root time which means runs only for half of divisors only 
  to slove the problem :-
    example of 21:
      divisors:-1,3,7,21
      here inner loop runs for 4 only
      only get 1,3
      identify carefully :-1,21  ;3,7  ;7,3 ;21,1  ; are pairs
      loop runs for 4 which means give 1 and 3
      by j!=n/j(for if the number is prefect square(ex:-25[5,5]) means duplicate 5 is added ) which give pair other number and that  pair is added by n/j
